How to maintain:
access Char.java and add a final Char with the info of the character,
characters there are grouped by region then by rarity and then sorted by version

If by some reason Genshin adds more nations/elements/weapons add those to genshin.Game's enums
edit character info with version past (6.1) if new information disproves what is written
edit maxVersion in Game.java if (and when) a new version max is created or implement a method that finds the new version max

add more arguments to existing commands if they are needed

algorithm for solve -1 is generated by chatGPT, feel free to add more with arguments -2/-3 or smth else
